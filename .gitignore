# --- Large posterior traces (> GitHub 100 MB limit) ---
models/phase2_popk_trace.nc
models/phase2_popk_trace_likelihood.nc
models/phase2_popk_trace_smoke.nc
*.nc

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.python-version
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# --- Build / output ---
build/
dist/
*.egg
.coverage
htmlcov/

# --- Optuna SQLite caches ---
*.sqlite
*.db

# --- Large intermediate model checkpoints ---
*.pkl
*.joblib

# --- Manuscript drafts, submissions, peer-review correspondence ---
# Deliberately excluded: the Data Availability Statement commits to sharing
# source code, synthetic data generation, and analysis pipelines -- not
# manuscript drafts, reviewer comments, or response letters.
manuscript/
Manuscript_*/

# --- Nested repository ---
# github_release/ has its own .git directory and a separate copy of data/,
# models/, results/ and utils/. Ignored so `git add -A` cannot commit it as a
# broken gitlink entry.
github_release/

# --- Local AI coding-assistant state (machine-specific, not project content) ---
.claude/
.grok/
.crush/
.antigravitycli/
